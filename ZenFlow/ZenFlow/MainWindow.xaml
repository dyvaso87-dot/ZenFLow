<Window x:Class="ZenFlow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZenFlow" Height="600" Width="960"
        Background="#0D0D0D">
    <Window.Resources>
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavBtnActive" TargetType="Button" BasedOn="{StaticResource NavBtn}">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="#111111"
                BorderBrush="#222222" BorderThickness="0,0,1,0">
            <StackPanel>
                <!-- Logo -->
                <StackPanel Margin="20,20,20,24">
                    <TextBlock Text="ZenFlow" Foreground="White"
                               FontSize="16" FontWeight="Medium"
                               />
                    <TextBlock Text="v1.0" Foreground="#444444"
                               FontSize="12" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Nav -->
                <Button Content="  Tablero"  Tag="Tareas"
                        Style="{StaticResource NavBtn}" Click="NavClick"/>
                <Button Content="  Enfoque"  Tag="Enfoque"
                        Style="{StaticResource NavBtn}" Click="NavClick"/>
                <Button Content="  Hábitos"  Tag="Habitos"
                        Style="{StaticResource NavBtn}" Click="NavClick"/>

                
            </StackPanel>
        </Border>

        <!-- CONTENIDO -->
        <Frame Grid.Column="1" x:Name="MainFrame"
               Background="#0D0D0D"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>