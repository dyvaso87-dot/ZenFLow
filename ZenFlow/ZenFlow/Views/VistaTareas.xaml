<Page x:Class="ZenFlow.Views.VistaTareas"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Tareas" Background="#F5F7FA">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ENCABEZADO -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Mis tareas" FontSize="24" FontWeight="Bold"
                       Foreground="#1F4E79"/>
            <TextBlock x:Name="TxtFecha" FontSize="12" Foreground="#888"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- AGREGAR TAREA -->
        <Border Grid.Row="1" Background="White" CornerRadius="10"
                Padding="16" Margin="0,0,0,20"
                Effect="{DynamicResource SombraCard}">
            <StackPanel>
                <TextBlock Text="Nueva tarea" FontSize="13" FontWeight="SemiBold"
                           Foreground="#1F4E79" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtTitulo" Grid.Column="0"
                             Height="36" Padding="10,8"
                             FontSize="13" Margin="0,0,10,0"
                             BorderBrush="#DDDDDD" BorderThickness="1"
                             Background="#FAFAFA"
                             Tag="Escribe una tarea..."/>
                    <DatePicker x:Name="DpFecha" Grid.Column="1"
                                Height="36" Margin="0,0,10,0"
                                BorderBrush="#DDDDDD"/>
                    <Button Grid.Column="2" Content="+ Agregar"
                            Height="36" Padding="16,0"
                            Background="#1F4E79" Foreground="White"
                            BorderThickness="0" FontSize="13"
                            Cursor="Hand" Click="AgregarTarea_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#1F4E79"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#185FA5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>

        <!-- LISTA DE TAREAS -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ListaTareas">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="8"
                                Margin="0,0,0,10" Padding="16,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Titulo}"
                                               FontSize="14" FontWeight="SemiBold"
                                               Foreground="#222"/>
                                    <TextBlock Text="{Binding FechaLimite, StringFormat='Vence: {0:dd/MM/yyyy}'}"
                                               FontSize="11" Foreground="#888"
                                               Margin="0,3,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="✓ Completar"
                                        Margin="8,0" Padding="12,6"
                                        FontSize="12" Cursor="Hand"
                                        Background="#E1F5EE" Foreground="#0F6E56"
                                        BorderThickness="0"
                                        Tag="{Binding Id}"
                                        Click="Completar_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#C1EDD8"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Grid.Column="2" Content="🗑 Eliminar"
                                        Padding="12,6" FontSize="12" Cursor="Hand"
                                        Background="#FCEBEB" Foreground="#A32D2D"
                                        BorderThickness="0"
                                        Tag="{Binding Id}"
                                        Click="Eliminar_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F5C6C6"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>